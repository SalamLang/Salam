# .github/codeql-config.yml

# Specify the languages to analyze
version: 2
languages:
  - cpp
  - python

# Define which queries to run
queries:
  - security-extended
  - security-and-quality
  - codeql/cpp
  - codeql/python

# Options to control the behavior of analysis
# You can specify specific queries or set thresholds here
