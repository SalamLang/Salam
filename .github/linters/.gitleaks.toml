title = "Salam gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Known public test vectors, not real secrets"
regexes = [
  # RFC 6455 (WebSocket) example handshake nonce/key, used verbatim in the spec.
  '''dGhlIHNhbXBsZSBub25jZQ==''',
]
