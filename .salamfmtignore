# Formatter test fixtures: intentionally unformatted input, paired with an
# expected .out file. Reformatting them in place would make the fixture
# pre-formatted and defeat the test (see tests_port/ + tools/bash/run-tests.sh
# wk_fmt, which runs the formatter on a throwaway copy, not these files).
tests/en/fmt/keyword_fields.salam
tests/en/fmt/messy_en.salam
tests/en/fmt/order_fix.salam
tests/en/fmt/typed_blocks.salam
tests/fa/fmt/messy.salam
tests/fa/fmt/persian_syntax.salam

# Local/generated artifact (git-ignored via editor/*), not hand-maintained source.
editor/examples_data.salam
