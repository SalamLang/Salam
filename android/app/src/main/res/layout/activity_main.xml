<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/brand"
    tools:context=".MainActivity">

    <!-- ناحیه محتوا: با inset های سیستم padding می‌شود تا وب‌ویو زیر status/navigation bar نرود -->
    <FrameLayout
        android:id="@+id/contentArea"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <WebView
            android:id="@+id/webView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/white" />

        <!-- نوار پیشرفت بارگذاری صفحات بعدی (بالای صفحه) -->
        <ProgressBar
            android:id="@+id/topProgress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:layout_gravity="top"
            android:indeterminateOnly="false"
            android:max="100"
            android:progressTint="@color/brand"
            android:visibility="gone"
            tools:progress="40"
            tools:visibility="visible" />

        <!-- صفحه خطا/آفلاین -->
        <LinearLayout
            android:id="@+id/errorView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/white"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="32dp"
            android:visibility="gone"
            tools:visibility="gone">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/error_no_connection_title"
                android:textColor="@color/black"
                android:textSize="18sp"
                android:textStyle="bold" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:gravity="center"
                android:text="@string/error_no_connection_message"
                android:textColor="#666666"
                android:textSize="14sp" />

            <Button
                android:id="@+id/retryButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:backgroundTint="@color/brand"
                android:paddingHorizontal="32dp"
                android:text="@string/retry"
                android:textColor="@color/white" />
        </LinearLayout>
    </FrameLayout>

    <!-- اسپلش اورلِی: تمام‌صفحه، لوگو + لودینگ. پس از بارگذاری سایت محو می‌شود -->
    <FrameLayout
        android:id="@+id/splashOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/brand">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center_horizontal"
            android:orientation="vertical">

            <ImageView
                android:layout_width="150dp"
                android:layout_height="150dp"
                android:contentDescription="@string/splash_logo_desc"
                android:src="@drawable/logo" />

            <ProgressBar
                android:id="@+id/splashProgress"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginTop="40dp"
                android:indeterminateTint="@color/white" />
        </LinearLayout>
    </FrameLayout>

</FrameLayout>
