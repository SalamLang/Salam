func main:
    mut sum f64 = 0.0
    mut sign f64 = 1.0
    mut den f64 = 1.0
    for mut k i64 = 0; k < 3000000; k = k + 1:
        sum = sum + sign / den
        sign = 0.0 - sign
        den = den + 2.0
    end
    pi f64 = 4.0 * sum
    v f64 = pi * 1000000.0
    __ v as i64
end
