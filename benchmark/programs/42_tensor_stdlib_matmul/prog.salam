/*
 * Salam Programming Language (2024-2026)
 *
 *   +-------------------+
 *   |     S A L A M     |
 *   +-------------------+
 *
 * Designed by Seyyed Ali Mohammadiyeh and the Salam Team
 * Born from a decade of language design experience (since 2018)
 *
 * Repository: https://github.com/SalamLang/Salam
 *
 */

// Tensor-stdlib benchmark: the f32 product from std/tensor, the kernel
// every neural network layer runs on, against NumPy's float32 matmul.
// Entries are small integers, so every product and partial sum is exact
// in f32 and the checksum matches the integer triple loops the other
// languages run.

import tensor

func main:
    n := 400
    mut a := tensor.Zeros2(n, n)
    mut b := tensor.Zeros2(n, n)
    mut i := 0
    until i < n:
        mut j := 0
        until j < n:
            a.set_at2(i, j, ((i * 3 + j * 7 + 1) % 97) as f64)
            b.set_at2(i, j, ((i * 5 + j * 2 + 3) % 89) as f64)
            j = j + 1
        end
        i = i + 1
    end
    c := tensor.MatMul(a, b)
    mut checksum := 0 as i64
    mut k := 0
    until k < c.numel():
        checksum = (checksum + (c.at(k) as i64)) % 1000000007
        k = k + 1
    end
    println checksum
end
