---
services:
  books:
    build:
      context: .
      dockerfile: Dockerfile
    image: salam-books:latest
    init: true
    user: '${HOST_UID:-1000}:${HOST_GID:-1000}'
    security_opt:
      - no-new-privileges:true
    volumes:
      - ..:/books
    working_dir: /books
    command: ['all']
