# Local development config: resolve std/ from this checkout instead of the
# installed prefix. Used by the compiler/salam bootstrap binary (and any
# binary built alongside it in compiler/). Path is relative to this file's
# directory (compiler/), so it works whether the binary was built natively
# on Windows, Linux, or under WSL.
stdlib = ../std
