استيراد دوم

دالة الرئيسية:
    صندوق := dom.Create("div")
    dom.SetAttr(صندوق, "id", "box")
    dom.SetText(صندوق, "hello dom")
    dom.Append(dom.Body(), صندوق)

    موجود := dom.ById("box")
    اطبع dom.GetText(موجود)

    dom.SetHtml(موجود, "<b>bold</b>")
    اطبع موجود != dom.Body()
نهاية
