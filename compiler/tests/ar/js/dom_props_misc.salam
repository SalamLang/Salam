استيراد دوم

دالة الرئيسية:
    dom.SetTitle("Salam Page")

    كائن := dom.Body()
    dom.SetNum(كائن, "score", 9.5)
    اطبع dom.GetNum(كائن, "score")
    dom.SetStr(كائن, "label", "top")
    اطبع dom.GetStr(كائن, "label")
    dom.SetBool(كائن, "active", صواب)
    اطبع dom.GetBool(كائن, "active")

    تأكيد := dom.Confirm("proceed?")
    اطبع تأكيد
    رد := dom.Prompt("your name?")
    اطبع رد
نهاية
