استيراد دوم

دالة الرئيسية:
    مدخل := dom.Create("input")
    dom.Append(dom.Body(), مدخل)
    dom.SetValue(مدخل, "hello")
    اطبع dom.GetValue(مدخل)
    dom.SetValue(مدخل, "world")
    اطبع dom.GetValue(مدخل)
نهاية
