// لغة: العربية
دالة تصنيف(ن : صحيح) : نص:
    أرجع ن < 0 ? "negative" : ن == 0 ? "zero" : "positive"
نهاية

دالة الرئيسية:
    اطبع تصنيف(-4)
    اطبع تصنيف(0)
    اطبع تصنيف(9)
    س := 7
    اطبع س > 5 ? "big" : "small"
نهاية
