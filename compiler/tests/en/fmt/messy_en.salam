// LANG: en
func main:
    name := "world"
    mut count := 0
    count += 3*4
    if count >= 10:
        __ name, count
    else:
        __ "small"
    end
    repeat 2 with i: __ "i", i
    end
end
