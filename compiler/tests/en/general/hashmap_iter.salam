func main:
    mut m HashMap<str, i32> = HashMap {}
    m.put("a", 10)
    m.put("b", 20)
    m.put("c", 30)
    mut total i32 = 0
    it auto = m.iter()
    while it.has_next():
        total = total + it.value()
        it.next()
    end
    __ total
end
