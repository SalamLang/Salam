import "http"
func main:
    mut h HashMap<str, str> = http.NewHeaders()
    h.put("Authorization", "Bearer xyz")
    h.put("Accept", "application/json")
    __ h.size()
    __ h.get("Authorization")
    __ h.get("Accept")
    __ h.has("Accept")
    __ h.has("Missing")
end
