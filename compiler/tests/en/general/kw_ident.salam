struct Resp:
    type str = ""
    mut i32 = 0
    if bool = false
end
func main:
    mut r Resp = Resp { type = "json", mut = 5, if = true }
    println (r.type, r.mut, r.if)
    r.type = "html"
    r.mut = 9
    println (r.type, r.mut, r.if)
end
