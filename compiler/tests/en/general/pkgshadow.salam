import "filepath"

func extlen(filepath : str) : int:
    ret filepath.len()
end

func main:
    __ filepath.Ext("a/b.c")
    __ extlen("hello.txt")
    filepath : str = "abc/def.txt"
    __ filepath.len()
    __ filepath.substr(0, 3)
end
