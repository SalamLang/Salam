import "db/sqlite"
func run():
    db : sqlite.Database = sqlite.Open(":memory:")
    sqlite.Exec(db, "CREATE TABLE t(v INTEGER)")
    __ sqlite.QueryInt(db, "SELECT v FROM t")
    __ sqlite.QueryText(db, "SELECT 'x' FROM t") == ""
    sqlite.Close(db)
end

func main:
    if sqlite.Available():
        run()
    else:
        __ 0
        __ true
    end
end
