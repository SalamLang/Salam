const VERSION := "0.2.0"
func main:
    println ("Salam/" + VERSION)
    println ("count = " + 42)
    println ("pi ~= " + 3.5)
    println (7 + " items")
    println ("ok? " + true)
    println ("grade: " + 'A')
    name := "world"
    println ("Hello, " + name + "! build " + 100)
    mut s := "a"
    s += "b"
    s += 1
    s += true
    println (s)
end
