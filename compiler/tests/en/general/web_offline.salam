import web

func h dummy(_ctx : i64): ret end

func main:
    r0 := web.NewRouter()
    println r0.routes.len() == 0
    mut r := web.NewRouter()
    web.Get(r, "/", funcptr(h dummy))
    web.Post(r, "/items", funcptr(h dummy))
    println r.routes.len() == 2
    println r.has static == false
    web.Static(r, "/static/", "./public")
    println r.has static == true
    println r.static prefix == "/static/"
    s := web.NewServer(8080)
    println s.port == 8080
    println s.hide tokens == false
end
