func classify(n : int) : str:
    ret n < 0 ? "negative" : n == 0 ? "zero" : "positive"
end

func main:
    println classify(-4)
    println classify(0)
    println classify(9)
    x := 7
    println x > 5 ? "big" : "small"
end
