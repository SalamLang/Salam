const STEP i32 = 5

func area(w i32, h i32 = 2) i32:
    ret w * h
end

func bump(x i32, by i32 = STEP) i32:
    ret x + by
end

func main:
    println(area(3))
    println(area(3, 4))
    println(bump(10))
    println(bump(10, 1))
end
