func noisy() : bool:
    __ "side"
    ret true
end

func main:
    x : int = 0

    if x != 0 && noisy():
        __ "and"
    end
    if x == 0 || noisy():
        __ "or"
    end

    if x != 0 && 10 / x > 0:
        __ "bad"
    end
    __ "done"
end
