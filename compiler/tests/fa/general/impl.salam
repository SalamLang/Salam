// زبان: فارسی
واسط توصیف:
    تابع برچسب() : رشته
    تابع امتیاز() : صحیح
پایان

پیاده‌سازی توصیف برای صحیح:
    تابع برچسب() : رشته: بازگشت "عدد" پایان
    تابع امتیاز() : صحیح: بازگشت این * 10 پایان
پایان

پیاده‌سازی توصیف برای منطقی:
    تابع برچسب() : رشته: بازگشت "پرچم" پایان
    تابع امتیاز() : صحیح:
        اگر این: بازگشت 1 پایان
        بازگشت 0
    پایان
پایان

تابع نمایش<ت: توصیف>(مقدار : ت):
    چاپ مقدار.برچسب()
    چاپ مقدار.امتیاز()
پایان

تابع اصلی:
    نمایش(7)
    نمایش(درست)
    نمایش(نادرست)
پایان
