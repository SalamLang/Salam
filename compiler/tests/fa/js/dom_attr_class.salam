واردکردن دام

تابع اصلی:
    عنصر := dom.Create("span")
    dom.Append(dom.Body(), عنصر)
    dom.SetAttr(عنصر, "data-role", "tag")
    چاپ dom.GetAttr(عنصر, "data-role")

    dom.AddClass(عنصر, "card")
    dom.AddClass(عنصر, "big")
    چاپ dom.GetAttr(عنصر, "class")

    dom.ToggleClass(عنصر, "big")
    چاپ dom.GetAttr(عنصر, "class")

    dom.ToggleClass(عنصر, "big")
    چاپ dom.GetAttr(عنصر, "class")

    dom.RemoveClass(عنصر, "card")
    چاپ dom.GetAttr(عنصر, "class")
تمام
