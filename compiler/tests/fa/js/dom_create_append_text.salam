واردکردن دام

تابع اصلی:
    جعبه := dom.Create("div")
    dom.SetAttr(جعبه, "id", "box")
    dom.SetText(جعبه, "hello dom")
    dom.Append(dom.Body(), جعبه)

    یافته := dom.ById("box")
    چاپ dom.GetText(یافته)

    dom.SetHtml(یافته, "<b>bold</b>")
    چاپ یافته != dom.Body()
تمام
