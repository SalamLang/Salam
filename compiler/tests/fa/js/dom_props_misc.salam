واردکردن دام

تابع اصلی:
    dom.SetTitle("Salam Page")

    شی := dom.Body()
    dom.SetNum(شی, "score", 9.5)
    چاپ dom.GetNum(شی, "score")
    dom.SetStr(شی, "label", "top")
    چاپ dom.GetStr(شی, "label")
    dom.SetBool(شی, "active", درست)
    چاپ dom.GetBool(شی, "active")

    تایید := dom.Confirm("proceed?")
    چاپ تایید
    پاسخ := dom.Prompt("your name?")
    چاپ پاسخ
تمام
