واردکردن دام

تابع اصلی:
    عنصر := dom.Create("div")
    dom.Append(dom.Body(), عنصر)
    dom.SetStyle(عنصر, "color", "red")
    dom.SetStyle(عنصر, "font-size", "12px")
    چاپ dom.GetStyle(عنصر, "color")
    چاپ dom.GetStyle(عنصر, "font-size")
    dom.SetStyle(عنصر, "color", "blue")
    چاپ dom.GetStyle(عنصر, "color")
تمام
