واردکردن دام

تابع اصلی:
    ورودی := dom.Create("input")
    dom.Append(dom.Body(), ورودی)
    dom.SetValue(ورودی, "hello")
    چاپ dom.GetValue(ورودی)
    dom.SetValue(ورودی, "world")
    چاپ dom.GetValue(ورودی)
تمام
