//! وضع: برنامج | عنوان: عكس النص
// لغة: العربية
دالة الرئيسية:
    س := "Salam"
    متغير ر := ""
    متغير i := س.len() - 1
    بينما i >= 0:
        ر = ر.concat(س.substr(i, 1))
        i = i - 1
    نهاية
    اطبع "عكس", س, "هو", ر
نهاية
