func main:
    mut s := Vector {} as Vector<int>
    s.push(1)
    s.push(2)
    s.push(3)
    println "size =", s.len()
    until s.len() > 0:
        println "pop", s.pop()
    end
    println "size =", s.len()
end
