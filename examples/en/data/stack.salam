func main:
    mut s Vector<i32> = Vector {}
    s.push(1)
    s.push(2)
    s.push(3)
    println "size =", s.len()
    while s.len() > 0:
        println "pop", s.pop()
    end
    println "size =", s.len()
end
