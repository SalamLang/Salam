//! mode: app | title: Reverse string
func main:
    s : str = "Salam"
    mut r : str = ""
    mut i : int = s.len() - 1
    while i >= 0:
        r = r.concat(s.substr(i, 1))
        i = i - 1
    end
    println "reverse of", s, "is", r
end
