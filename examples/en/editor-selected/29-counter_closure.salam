//! mode: app | title: Counter closure
func make_counter() : func () int:
    mut n := 0
    ret () =>:
        n = n + 1
        ret n
    end
end

func main:
    next := make_counter()
    println next(), next(), next(), next()
end
