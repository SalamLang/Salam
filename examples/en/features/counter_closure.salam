func make_counter() : func () int:
    mut n : int = 0
    ret () =>:
        n = n + 1
        ret n
    end
end

func main:
    next : func () int = make_counter()
    println next(), next(), next(), next()
end
