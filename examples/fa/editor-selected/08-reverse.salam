//! حالت: برنامه | عنوان: وارونه‌سازی رشته
تابع اصلی:
    س := "Salam"
    متغیر ر := ""
    متغیر i := س.len() - 1
    تاوقتی i >= 0:
        ر = ر.concat(س.substr(i, 1))
        i = i - 1
    تمام
    چاپ "وارونه‌ی", س, "می‌شود", ر
تمام
