//! حالت: app | عنوان: وارونه‌سازی رشته
تابع اصلی:
    س رشته = "Salam"
    متغیر ر رشته = ""
    متغیر i صحیح = س.len() - 1
    تاوقتی i >= 0:
        ر = ر.concat(س.substr(i, 1))
        i = i - 1
    پایان
    چاپ "وارونه‌ی", س, "می‌شود", ر
پایان
