//! mode: app | title: پشته
تابع اصلی:
    متغیر پشته Vector<صحیح۳۲> = Vector {}
    پشته.push(1)
    پشته.push(2)
    پشته.push(3)
    چاپ "اندازه =", پشته.len() بعنوان صحیح۳۲
    تاوقتی پشته.len() > 0:
        چاپ "برداشت", پشته.pop()
    پایان
    چاپ "اندازه =", پشته.len() بعنوان صحیح۳۲
پایان
