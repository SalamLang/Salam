//! حالت: برنامه | عنوان: پشته
تابع اصلی:
    متغیر پشته := Vector {} بعنوان Vector<صحیح>
    پشته.push(1)
    پشته.push(2)
    پشته.push(3)
    چاپ "اندازه =", پشته.len() بعنوان صحیح
    تاوقتی پشته.len() > 0:
        چاپ "برداشت", پشته.pop()
    تمام
    چاپ "اندازه =", پشته.len() بعنوان صحیح
تمام
