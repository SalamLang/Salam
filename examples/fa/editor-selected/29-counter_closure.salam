//! حالت: برنامه | عنوان: بستار شمارنده
تابع ساخت_شمارنده() : تابع () صحیح:
    متغیر ن : صحیح = 0
    بازگشت () =>:
        ن = ن + 1
        بازگشت ن
    پایان
پایان

تابع اصلی:
    بعدی : تابع () صحیح = ساخت_شمارنده()
    چاپ بعدی(), بعدی(), بعدی(), بعدی()
پایان
