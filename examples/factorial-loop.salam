تابع فاکتوریل(عدد) {
    نتیجه = ۱
    شمارنده = ۱

    تا شمارنده != عدد + ۱ {
        نتیجه = نتیجه * شمارنده
        شمارنده = شمارنده + ۱
    }
    برگشت نتیجه
}

تابع سلام {
//    نمایش فاکتوریل(5)

    عددد = ۱۰
    نمایش عددد

نمایش زوج(عدد)

    نمایش فاکتوریل(عددد)
}
