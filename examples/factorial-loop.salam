تابع فاکتوریل(عدد) {
    نتیجه = ۱
    اندیس = ۱

    تا اندیس != عدد + ۱ {
        نتیجه = نتیجه * اندیس
        اندیس = اندیس + ۱
    }
    برگشت نتیجه
}

تابع سلام {
//    نمایش فاکتوریل(5)

    عددد = ۱۰
    نمایش عددد

نمایش زوج(عدد)

    نمایش فاکتوریل(عددد)
}
