تابع فاکتوریل(عدد) {
    نتیجه = ۱
    اندیس = ۱

    تا اندیس != عدد + ۱ {
        نتیجه = نتیجه * اندیس
        اندیس = اندیس + ۱
    }

    برگشت نتیجه
}

تابع سلام {
    نمایش فاکتوریل(5)

    عدد = ۱۰
    نمایش فاکتوریل(عدد)
}
