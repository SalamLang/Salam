عملکرد فاکتوریل(عدد) {
    اگر عدد == ۰ {
        برگشت ۱
    }
    برگشت عدد * فاکتوریل(عدد - ۱)
}


عملکرد تست() {
    عدد = ۱۰
    نتیجه = فاکتوریل(عدد)
    نمایش نتیجه

    نمایش "فاکتوریل از " + عدد + " برابر با " + نتیجه

}

عملکرد سلام {
    عدد = ۵
    نمایش فاکتوریل(عدد)

    تست()
}