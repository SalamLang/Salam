عملکرد فاکتوریل(عدد) {
    اگر عدد == ۰ {
        برگشت ۱
    }
    برگشت عدد * فاکتوریل(عدد - ۱)
}


عملکرد تست() {
    عدد = ۵
    نتیجه = فاکتوریل(عدد)
}


عملکرد سلام {
    عدد = ۵
    نمایش فاکتوریل(عدد)

    تست()
}