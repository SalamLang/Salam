عملکرد سلام {
    // name = "Max"
    نام = "مکس"

    // print type(name)
    نمایش نوع(نام)

    // if type(name) == "STRING" {}
    اگر نوع(نام) == "STRING" {
        نمایش "بله متن است
    // else {}
    } واگرنه {
        نمایش "خیر اشتباه است"
    }
}
