تابع سلام {
    // name = "Max"
    نام = "مکس"

    // print type(name)
    نمایش نوع(نام)

    // if type(name) == "رشته" {}
    اگر نوع(نام) == "رشته" {
        نمایش "بله متن است
    // else {}
    } واگرنه {
        نمایش "خیر اشتباه است"
    }
}
