// function main {}
عملکرد سلام {
    // value = 1
    مقدار = ۱
    // print value
    نمایش مقدار
    {
        // print value
        نمایش مقدار
        {
            // print value
            نمایش مقدار
            value = value + 1
            مقدار = مقدار + ۱
            // print value
            نمایش مقدار
        }
        // print value
        نمایش مقدار
    }
    // print value
    نمایش مقدار
}
