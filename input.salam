عملکرد فاکتوریل(عدد)
{
    اگر عدد == ۱ یا عدد == ۰ {
        برگشت ۱
    }
    واگرنه {
        برگشت عدد * فاکتوریل(عدد - ۱)
    }
}

عملکرد تست {
    برگشت فاکتوریل(۱۰)
}

عملکرد سلام() {
    نمایش تست()
}
