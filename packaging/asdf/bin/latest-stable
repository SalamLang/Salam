#!/usr/bin/env bash
set -euo pipefail

curl -fsSL \
    ${GITHUB_API_TOKEN:+-H "Authorization: token $GITHUB_API_TOKEN"} \
    "https://api.github.com/repos/SalamLang/Salam/releases/latest" \
    | grep -o '"tag_name": *"v[^"]*"' \
    | sed 's/.*"v\([^"]*\)"/\1/'
