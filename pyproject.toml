[project]
name = "salam"
version = "0.2.2"
requires-python = ">=3.13"

[tool.codespell]
ignore-words = ".github/linters/codespell.txt"
skip = "runner/worker-configuration.d.ts,bun.lock"

[tool.mypy]
exclude = "^benchmark/programs/"
