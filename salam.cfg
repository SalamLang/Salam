# Local development config: resolve std/ from this checkout instead of the
# installed prefix. Used by the ./salam binary built into the repo root.
# Path is relative to this file's directory (the repo root), so it works
# whether ./salam was built natively on Windows, Linux, or under WSL.
stdlib = compiler/std
