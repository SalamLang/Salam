import { Generator } from './../generator';
import { AstStatementReturn } from '../../../parser/parse/ast/statement/return';

export function generateStatementReturn(generator: Generator, stmt: AstStatementReturn): string {
    let result: string = "";
    return result;
};
