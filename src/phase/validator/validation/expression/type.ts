import { Validator } from "./../validator";
import { AstType } from "../../../parser/parse/ast/expression/type";

export function validateType(validator: Validator, node: AstType): void {
    // TODO: handling custom struct and user-defined types
};
