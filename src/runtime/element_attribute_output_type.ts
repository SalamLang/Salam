export enum RuntimeElementAttributeOutputType {
    Normal = 'Normal',
    Url = 'Url', // for `background-image` attribute or `font` CSS attribute
    UrlWithFont = 'UrlWithFont', // for `font-family`
    // TODO: add more output types
};
