Compile-checked only. main.salam is a server: http.Run never returns, so there
is no output for the runner to compare. The measurements that justify this
program live in BENCHMARK.md and are produced by bench/run.sh, which needs
php, node and nginx and is not part of the default suite.
