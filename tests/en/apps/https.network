This test performs live HTTPS requests (example.com, api.github.com) and is
skipped by run-tests.sh unless SALAM_TEST_NETWORK=1 is set, since it is
flaky under GitHub Actions' shared egress IPs and GitHub's unauthenticated
API rate limit.
