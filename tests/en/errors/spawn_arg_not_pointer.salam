/*
 * Salam Programming Language (2024-2026)
 *
 *   +-------------------+
 *   |     S A L A M     |
 *   +-------------------+
 *
 * Designed by Seyyed Ali Mohammadiyeh and the Salam Team
 * Born from a decade of language design experience (since 2018)
 *
 * Repository: https://github.com/SalamLang/Salam
 *
 */

// EXPECT: E012
// The payload travels through pthread_create's void* slot, so the worker's
// one parameter has to be a pointer - an i64 cannot carry it.
func worker(n: i64):
    println n
end

func main:
    t := spawn(worker, 7 as i64)
    join(t)
end
