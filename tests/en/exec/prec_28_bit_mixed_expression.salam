/*
 * Salam Programming Language (2024-2026)
 *
 *   +-------------------+
 *   |     S A L A M     |
 *   +-------------------+
 *
 * Designed by Seyyed Ali Mohammadiyeh and the Salam Team
 * Born from a decade of language design experience (since 2018)
 *
 * Repository: https://github.com/SalamLang/Salam
 *
 */

// One long expression exercising the whole ladder at once:
// ** > unary > as > * / % > + - > << >> > relational > equality >
// & > ^ > | > && > ||.

func main:
    println 1 | 2 ^ 3 & 4 + 5 * 2 >> 1
    println 1 | (2 ^ (3 & ((4 + (5 * 2)) >> 1)))
    println ~1 & 3 | 4 ^ 2 << 1
    println ((~1) & 3) | (4 ^ (2 << 1))
    println (2 ** 3 as int) | 1
    // equality binds tighter than &, so `7 & 3 == 3` is `7 & (3 == 3)`
    // and does not type-check; the mask test needs its own parentheses
    println (7 & 3) == 3
    println (7 & 8) == 0
    println 1 << 2 == 4 && 8 >> 1 == 4 || false
    println ((1 << 2) == 4) && ((8 >> 1) == 4) || false
    mut x := 5
    x |= 3 & 6 ^ 1 << 3
    println x
end
