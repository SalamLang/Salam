/*
 * Salam Programming Language (2024-2026)
 *
 *   +-------------------+
 *   |     S A L A M     |
 *   +-------------------+
 *
 * Designed by Seyyed Ali Mohammadiyeh and the Salam Team
 * Born from a decade of language design experience (since 2018)
 *
 * Repository: https://github.com/SalamLang/Salam
 *
 */

// `while` is the same loop as `until`, under the name that says what it
// does: the body runs *while* the condition holds. Both spellings compile to
// the same thing, and a file may use either.
package main

func main:
    mut i := 0
    while i < 3:
        print "w", i, " "
        i += 1
    end
    println ""

    mut j := 3
    until j > 0:
        print "u", j, " "
        j -= 1
    end
    println ""

    mut k := 0
    while true:
        k += 1
        if k == 2: break end
    end
    println k
end
