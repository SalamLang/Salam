/*
 * Salam Programming Language (2024-2026)
 *
 *   +-------------------+
 *   |     S A L A M     |
 *   +-------------------+
 *
 * Designed by Seyyed Ali Mohammadiyeh and the Salam Team
 * Born from a decade of language design experience (since 2018)
 *
 * Repository: https://github.com/SalamLang/Salam
 *
 */

type IntFn = func (int) int

struct Pipeline:
    pub stages: Vector<IntFn>

    pub func run(x: int): int:
        mut v := x
        each st in this.stages:
            v = st(v)
        end
        ret v
    end
end

func main:
    fs := Vector {} as Vector<func (int) int> repeat 3 with k: fs.push((v: int) => v * (k + 1))
end
println fs.get(2)(10)
println fs.len()
fs.free()

m := HashMap {} as HashMap<str, func (int) int> m.put("dbl", (v: int) => v * 2)
m.put("neg", (v: int) => 0 - v)
dbl := m.get("dbl")
neg := m.get("neg")
println dbl(21), neg(5)
m.free()

p := Pipeline { stages = Vector {} as Vector<IntFn> }
p.stages.push((v: int) => v + 1)
p.stages.push((v: int) => v * 10)
println p.run(4)
p.stages.free()
end
