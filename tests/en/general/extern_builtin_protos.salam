/*
 * Salam Programming Language (2024-2026)
 *
 *   +-------------------+
 *   |     S A L A M     |
 *   +-------------------+
 *
 * Designed by Seyyed Ali Mohammadiyeh and the Salam Team
 * Born from a decade of language design experience (since 2018)
 *
 * Repository: https://github.com/SalamLang/Salam
 *
 */

// An `extern` that names a function gcc and clang know as a built-in is
// declared with the C library's own prototype, whatever the Salam
// signature says, so the C compiler never sees a second, mismatched
// declaration (-Wbuiltin-declaration-mismatch).

extern:
    func abs(x: i64): i64
    func labs(x: i64): i64
    func llabs(x: i64): i64
    func strdup(s: str): str
    func strndup(s: str, n: i32): str
    func atoi(s: str): i32
    func strchr(s: str, c: i32): str
end

func main:
    println abs(-3), labs(-4), llabs(-5)
    println strdup("copy"), strndup("truncate", 5), atoi("42")
    println strchr("hello", 108)
end
