/*
 * Salam Programming Language (2024-2026)
 *
 *   +-------------------+
 *   |     S A L A M     |
 *   +-------------------+
 *
 * Designed by Seyyed Ali Mohammadiyeh and the Salam Team
 * Born from a decade of language design experience (since 2018)
 *
 * Repository: https://github.com/SalamLang/Salam
 *
 */

// A bare function name read as a value is its address, typed i64 - the slot
// std/net/router and the other handler registries take. Covers all three
// shapes: bound to a local, passed straight to callhandler, and handed to
// another function that calls it back.

func greet(x: i64):
    println "greet", x
end

func twice(x: i64):
    println "twice", x * 2
end

func dispatch(fn: i64, arg: i64):
    callhandler(fn, arg)
end

func main:
    h := greet
    callhandler(h, 1)
    callhandler(greet, 2)
    dispatch(twice, 3)
    ret 0
end
