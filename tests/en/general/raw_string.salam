func main:
    __ `hello world`
    __ `no\n escape here`
    __ `contains "double" quotes`
    __ `contains 'single' quotes`
    __ `tab\there`
    prefix auto = `Path: `
    __ prefix + "/usr/bin"
    __ `first` + ` ` + `second`
end
