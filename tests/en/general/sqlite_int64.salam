import "db/sqlite"
func run():
    db sqlite.Database = sqlite.Open(":memory:")
    sqlite.Exec(db, "CREATE TABLE t(v INTEGER)")
    sqlite.Exec(db, "INSERT INTO t VALUES (10000000000)")
    r sqlite.Rows = sqlite.Query(db, "SELECT v FROM t")
    sqlite.Next(r)
    __ sqlite.Int64(r, 0)
    sqlite.Finish(r)
    sqlite.Close(db)
end
func main:
    if sqlite.Available():
        run()
    else:
        __ 10000000000
    end
end
