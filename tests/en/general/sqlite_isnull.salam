import "db/sqlite"
func run():
    db sqlite.Database = sqlite.Open(":memory:")
    sqlite.Exec(db, "CREATE TABLE t(v INTEGER)")
    sqlite.Exec(db, "INSERT INTO t(v) VALUES (NULL), (7)")
    r sqlite.Rows = sqlite.Query(db, "SELECT v FROM t ORDER BY rowid")
    sqlite.Next(r)
    __ sqlite.IsNull(r, 0)
    sqlite.Next(r)
    __ sqlite.IsNull(r, 0)
    sqlite.Finish(r)
    sqlite.Close(db)
end
func main:
    if sqlite.Available():
        run()
    else:
        __ true
        __ false
    end
end
