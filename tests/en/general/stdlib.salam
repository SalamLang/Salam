import (
    "math"
    "str"
)
func main:
    __ math.Sqrt(16.0)
    __ math.Pow(2.0, 10.0)
    __ str.Len("hello")
    __ str.Compare("a", "b")
    __ str.Concat("foo", "bar")
    parts Vector<str> = str.Split("a,b,c,d", ",")
    __ parts.len()
    parts.free()
end
