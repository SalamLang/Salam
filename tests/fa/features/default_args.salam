/*
 * Salam Programming Language (2024-2026)
 *
 *   +-------------------+
 *   |     S A L A M     |
 *   +-------------------+
 *
 * Designed by Seyyed Ali Mohammadiyeh and the Salam Team
 * Born from a decade of language design experience (since 2018)
 *
 * Repository: https://github.com/SalamLang/Salam
 *
 */

پایا سلام := "سلام"

ساختار پیکربندی:
    همگانی میزبان: رشته = "localhost"
    همگانی درگاه: صحیح = 8080
    همگانی امن: منطقی = نادرست
پایان

روال درود(نام: رشته, پیشوند: رشته = سلام, بار: صحیح = 1):
    تکرار بار:
        سرچاپ پیشوند, نام
    پایان
پایان

روال توضیح(پ: پیکربندی):
    سرچاپ پ.میزبان, پ.درگاه, پ.امن
پایان

روال ریشه:
    درود("سارا")
    درود("علی", "های")
    درود("مکس", "هی", 3)

    توضیح(پیکربندی {})
    توضیح(پیکربندی { درگاه = 9090 })
    توضیح(پیکربندی { میزبان = "example.com", امن = درست })
پایان
