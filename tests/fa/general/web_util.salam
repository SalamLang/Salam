/*
 * Salam Programming Language (2024-2026)
 *
 *   +-------------------+
 *   |     S A L A M     |
 *   +-------------------+
 *
 * Designed by Seyyed Ali Mohammadiyeh and the Salam Team
 * Born from a decade of language design experience (since 2018)
 *
 * Repository: https://github.com/SalamLang/Salam
 *
 */

واردسازی شبکه.اچ تی تی پی شبکه
واردسازی شبکه.مایم شبکه

روال ریشه:
    سرچاپ اچ تی تی پی شبکه.رمزگشایی نشانی("a%20b") == "a b"
    سرچاپ اچ تی تی پی شبکه.رمزگشایی نشانی("a+b") == "a b"
    سرچاپ اچ تی تی پی شبکه.رمزگشایی نشانی("100%25") == "100%"
    سرچاپ اچ تی تی پی شبکه.رمزگشایی نشانی("name=Ali%20Reza") == "name=Ali Reza"
    سرچاپ اچ تی تی پی شبکه.رمزگشایی نشانی("plain") == "plain"

    سرچاپ مایم شبکه.پسوند مایم("html") == "text/html; charset=utf-8"
    سرچاپ مایم شبکه.پسوند مایم("css") == "text/css"
    سرچاپ مایم شبکه.پسوند مایم("js") == "application/javascript"
    سرچاپ مایم شبکه.پسوند مایم("png") == "image/png"
    سرچاپ مایم شبکه.پسوند مایم("unknownext") == "application/octet-stream"

    سرچاپ مایم شبکه.رده مایم("/assets/site.css") == "text/css"
    سرچاپ مایم شبکه.رده مایم("/img/logo.png") == "image/png"
    سرچاپ مایم شبکه.رده مایم("archive.tar.gz") == "application/octet-stream"
    سرچاپ مایم شبکه.رده مایم("noextension") == "application/octet-stream"

    سرچاپ مایم شبکه.متن وضعیت(200) == "OK"
    سرچاپ مایم شبکه.متن وضعیت(201) == "Created"
    سرچاپ مایم شبکه.متن وضعیت(404) == "Not Found"
    سرچاپ مایم شبکه.متن وضعیت(500) == "Internal Server Error"
    سرچاپ مایم شبکه.متن وضعیت(799) == "Unknown"

    سرچاپ اچ تی تی پی شبکه.نسخه سرور == SALAM_VERSION
پایان
