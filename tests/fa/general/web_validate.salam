/*
 * Salam Programming Language (2024-2026)
 *
 *   +-------------------+
 *   |     S A L A M     |
 *   +-------------------+
 *
 * Designed by Seyyed Ali Mohammadiyeh and the Salam Team
 * Born from a decade of language design experience (since 2018)
 *
 * Repository: https://github.com/SalamLang/Salam
 *
 */

واردسازی شبکه.اچ تی تی پی شبکه.اعتبارسنجی

روال ریشه:
    سرچاپ اعتبارسنجی.الزامی("hello") == درست
    سرچاپ اعتبارسنجی.الزامی("") == نادرست

    سرچاپ اعتبارسنجی.طول("abc", 1, 5) == درست
    سرچاپ اعتبارسنجی.طول("abc", 3, 3) == درست
    سرچاپ اعتبارسنجی.طول("", 1, 5) == نادرست
    سرچاپ اعتبارسنجی.طول("toolong", 1, 5) == نادرست

    سرچاپ اعتبارسنجی.ایمیل است("user@example.com") == درست
    سرچاپ اعتبارسنجی.ایمیل است("no-at-sign.com") == نادرست
    سرچاپ اعتبارسنجی.ایمیل است("no-dot@example") == نادرست
    سرچاپ اعتبارسنجی.ایمیل است("") == نادرست
پایان
